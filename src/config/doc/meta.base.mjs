export const defaultEvents = {
  ready: 'Gets fired when plugin is ready',
  enable: 'Gets fired when plugin is enabled',
  disable: 'Gets fired when plugin is disabled',
  destroy: 'Gets fired when plugin is destroy',
  update: 'Gets fired when plugin is destroy',
  change: 'Gets fired when plugin is changed'
}

export const defaultMethods = {
  init: 'Initial plugin',
  enable: 'Enabled plugin if plugin is disabled',
  disable: 'Disable plugin',
  destroy: 'Destroy plugin',
  get: 'Get value by key',
  set: 'Set value by key',
  val: 'Set or get value by key'
}

export const defaultOptions = {
  theme: 'Add plugin theme option'
}

export const defaultDependencies = {
  anime: 'Anime (/ˈæn.ə.meɪ/) is a lightweight JavaScript animation library.',
  Hammer: 'A JavaScript library for detecting touch gestures.',
  Tether: 'A positioning engine to make overlays, tooltips and dropdowns better'
}

export const defaultClasses = {
  NAMESPACE: 'Declare plugin namespace',
  CONTAINER: 'Declare plugin range',
  ACTIVE: 'Announce plugin is actived',
  DISABLED: 'Announce plugin is disabled',
  HOVERING: 'Announce plugin is hoverd',
  THEME: 'Declare plugin theme'
}